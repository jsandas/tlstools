apiVersion: v1
kind: Service
metadata:
  name: {{ include "tlstools.fullname" . }}
  labels:
    {{- include "tlstools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tlstools.selectorLabels" . | nindent 4 }}
